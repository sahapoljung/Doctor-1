<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="th.ac.kmutnb.doctor">

    <uses-permission android:name="android.permission.INTERNET" />

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_doctorr"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SickDataActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".AnaFinalActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".DatasickMainActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".AnalysisActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".DocTorActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>